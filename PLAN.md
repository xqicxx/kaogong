# 考公知识系统 · 方案 v0.2

> 2026-09-13 · 全部数字为本机实测
> ✅ = 已定并跑通　🔬 = 方案，待你拍板

## 0. 现状盘点：能复用的都已有，别重造

- **Obsidian 库** `/Users/cx/Documents/Obsidian Vault`
  已装：dataview / tasks / templater / quickadd / **smart-connections**（本地语义检索）/ kanban / calendar / obsidian-git …
  → 面板、模板、捕获流程、语义关联，**四样都不用写代码**
- **语义检索** qmd 已把该库索引成 collection `notes`
  模型：**Qwen3-Embedding-0.6B**（向量）+ Qwen3-Reranker（重排），BM25 打底
  → 知识点 ↔ 错题的语义关联**是现成的**
- **定时 + 通知** launchd + Telegram 通道已在用
- **OCR** macOS Vision（系统自带，零安装、零模型下载）

---

## 1. 任务一：拍照 → Markdown 进 Obsidian　✅ 已跑通

### 1.1 引擎选型：macOS Vision

对比过几种方案，最后选了**系统自带的 Vision**：

| 方案 | 单张耗时 | 峰值内存 | 与基准相似度 |
| --- | --- | --- | --- |
| 方案 A（本地 VLM 1.2B） | ~20s | 1741 MB | — |
| 方案 B（pipeline：版面 + OCR） | 6.1s | 1132 MB | 0.994 |
| **macOS Vision** | **0.64s** | **152 MB** | **1.000** |

- Vision **快 10 倍、省内存 7.4 倍，准确率还高一点**
- Vision 是系统自带：**零模型下载、零 venv、单文件 93KB**
- 其余方案连同各自的模型缓存和运行环境已**全部删除（共释放 5.8G）**
- 代价：Vision **没有版面结构** → 版面得自己推（见 1.4）

### 1.2 最终流程（已定）

```text
拍照（手机）
   ↓  发 Telegram / AirDrop
① 矫正     Vision 文档检测找四角 + 透视校正            0.2s
② 去红笔   R−max(G,B) > 38 的像素抹白                 内置
③ OCR      mac Vision（文本行 + 坐标 + 置信度）         0.6s
④ 版面推断 几何 + 模式 → markdown（标题/段落/列表/去页眉页脚）
⑤ 校准     对着原图逐句核；拿不准就裁切放大 2–2.5× 再看
⑥ 落库     考公/<科目>/…；原始识别件留 _raw/
```

**一条命令：**

```bash
~/.pi/scripts/kaogong-ocr.sh --source "逻辑论证-归因论证" --module 行测/判断推理 图1.jpg 图2.jpg ...
# 机器产出 → 考公/_raw/行测/判断推理/      校对后 → 考公/行测/判断推理/
# --module 省略则落到 考公/_inbox/；非法模块直接报错并列出可选值
```

目录约定见 **1.11**。

**为什么必须有 ⑤：** 实测每页会残留 3–5 个错字（详细见 1.7），法律条文那种“错一个字换答案”的材料不能信机器。
校准成本很低——**机器负责把字从图里搬出来，人（我）负责对图**。

### 1.3 拍照必须矫正（躲不掉）

| 输入 | 相似度 |
| --- | --- |
| PDF 直出（基准） | 1.000 |
| 手拍 · 不矫正 | **0.807** |
| 手拍 · 矫正后 | **1.000** |

矫正 = `VNDetectDocumentSegmentationRequest`（自动找四角）+ `CIPerspectiveCorrection`
实测置信度 0.99、0.65s/张、零依赖。

### 1.4 版面推断规则（vision2md.py 已实现）

```text
标题   模式（第X章 / 第X节 / X.X / 一、 / （一） / 1. / （1））
       + 几何（行高 > 中位×1.18 且短 且无句末标点）
段落   续行合并；上一行以 。！？ 结尾且没占满栏宽 → 断段；间距过大或缩进 → 断段
列表   ①②③ / 数字括号 → 列表项，续行自动接上（中文不加空格）
噪声   ① 批内重复出现在页眉页脚带的短行（按相似度聚类，容忍各页识别出入）
       ② 页边带里的纯页码样式（第X页 / 数字）
       ③ 页边带里的商标英文大写行
       ⚠️ 命中标题模式的行永不删（踩过：真标题被当水印删掉）
fm     source / page / ocr / ocr_lines / ocr_uncertain / characters / proofread
```

### 1.5 实测数据

```text
矫正 0.2s + 去红笔 + OCR 0.6s   →   约 0.7s / 张
20 张一轮 ≈ 15 秒
```

### 1.6 真实照片实测（你的两页讲义，1760×2560 手机拍）

| 变体 | 真词命中（第1页 / 第2页） | 错词数 |
| --- | --- | --- |
| 原图 | 12/16　13/16 | 12 |
| **去红笔（默认）** | 11/16　**15/16** | **9** |
| 匀光 + 去红 | 9/16　1/16 | 文本被毁，已弃用 |

**去红笔有用，去阴影有害：** 红笔标注会把 `三圈质疑` 认成 `三西质艇`、`削弱` 认成 `削羽`；
抹掉红色像素后这些错全消失、**字数不变（无内容损失）**。
而「原图 ÷ 大半径模糊」的匀光会把浅色文字一起洗掉（第 2 页真词命中 13/16 → 1/16）→ **弃用**。

### 1.7 错误源排序（重要）

```text
① 纸张背面透印（头号元凶）—— 书页薄，背面那段文字镜像叠在正文上
   → 目前靠“对着原图校准”兜底；拍摄上垫黑纸能直接压住
② 红笔标注 —— 已由去红笔解决
③ 页边阴影 / 页脚暗角 —— 靠拍摄改善
④ 字体字形混淆（入/人、殊/珠、千/干）—— 靠校准
```

### 1.8 拍摄建议（直接降错误率）

```text
1. 书页背后垫一张黑卡/黑纸（挡背面反光）—— 压透印最有效
2. 光从两侧打，别用顶光
3. 页脚别压进阴影（最后一行最容易烂）
4. 分辨率再高点；现在 1760×2560 对密排小字偏紧
5. 红笔随便标注 —— 去红笔已内置
```

### 1.9 工具清单（源码在仓库，二进制装到 ~/.pi/bin）

```text
仓库 ~/code/kaogong/                  https://github.com/xqicxx/kaogong
  tools/*.swift       rectify / deink / vision-ocr / crop 源码
  tools/install.sh    编译并装到 ~/.pi/bin（零第三方依赖）
  pipeline/split.py   页 md → 骨架笔记 + 考点卡片
  pipeline/review.py  FSRS 排期 + Telegram 推送
  skills/             两个 skill 的副本（装到 ~/.pi/agent/skills/）

运行时
  ~/.pi/bin/rectify          自动找四角 + 透视矫正
  ~/.pi/bin/deink            去红笔（--flatten 匀光实测有害，别开）
  ~/.pi/bin/vision-ocr       OCR（--json 带坐标置信度，--plain 关语言纠正）
  ~/.pi/bin/crop             裁切 + 放大（校准用）
  ~/.pi/bin/vision2md.py     版面推断 → markdown
  ~/.pi/bin/kaogong-ocr.sh   一条命令串起来
```

### 1.10 已流程化成 skill

```text
~/.pi/agent/skills/kaogong-ingest/  ①拍照 → 校准 → 落库 → 切卡片（四条硬规则、错误源排序）
~/.pi/agent/skills/kaogong-review/  ②排期 → 推送 → 评分语义 → 三条证据（逾期优先/语义错开/题型交错）
```

---

### 1.11 Obsidian 目录结构（已建好）

```text
考公/
├── 行测/
│   ├── 常识判断/
│   ├── 言语理解与表达/
│   ├── 数量关系/
│   ├── 判断推理/
│   └── 资料分析/
├── 申论/
│   ├── 归纳概括/
│   ├── 综合分析/
│   ├── 提出对策/
│   ├── 贯彻执行/
│   └── 申发论述/
├── 错题/          错题本（任务三用）
├── _inbox/        没指定模块时的落点
└── _raw/          机器原始识别件，按 <模块> 镜像（proofread: false）
```

**两层约定：**

```text
考公/_raw/<模块>/<讲义>-pNNN.md    机器产出，未校对（proofread: false）
考公/<模块>/<讲义>-pNNN.md         校对之后的版本 ← 平时看这个
```

原始件永不删，是为了任何时候都能对回机器原文 —— 分得清是识别错、还是我校错。

**已归档的示例**（你那两页讲义）：

```text
考公/行测/判断推理/逻辑论证-归因论证-p001.md      ← 校准版
考公/行测/判断推理/逻辑论证-归因论证-p002.md
考公/_raw/行测/判断推理/逻辑论证-归因论证-p001.md  ← 机器原样
考公/_raw/行测/判断推理/逻辑论证-归因论证-p002.md
```

---

## 2. 任务二：什么时候复习、复习什么　🔬 待拍板

### 2.1 证据基础（Google Scholar 前沿检索，2026-09-13）

#### A. 间隔怎么排

| 结论 | 出处 | 备注 |
| --- | --- | --- |
| **最优间隔 ≈ 保持期的 10–20%** | Cepeda et al. 2008, Psych Science（引 1100+） | 6 个月保持 → 间隔约 3 周 |
| 记忆模型与排期**解耦**（随机最短路） | SSP-MMC, KDD 2022（墨墨，3 亿+ 记录） | 工业级验证过 |
| DSR（难度/稳定度/可提取度）+ 可训练遗忘曲线 | FSRS-6（Anki 24.11+ 默认） | 同保持率**少 20–30%** 复习量 |
| **语义干扰会毁掉间隔效果** → 用 LLM 语义相似度把相近概念错开排 | LECTOR, arXiv 2508.03275（2025） | 90.2% vs 最强基线 88.4% |
| 最优复习策略（点过程建模） | Tabibian et al. 2019, PNAS（引 303） | 同样复习量、保持率更高 |
| 技能级建模 + 时间窗 | DAS3H 2019（引 154） | 按技能而非按卡片排 |
| 深度 RL 排期 | DRL-SRS 2024 | 实验性强 |

#### B. 复习什么、怎么复

| 结论 | 出处 |
| --- | --- |
| 检索（自测/做题）> 重读，效应量 g ≈ 0.5 | Yang et al. 2021, Psych Bulletin（引 553）元分析 |
| 间隔 + 检索 = 两个最稳健的学习技术 | Carpenter et al. 2022, Nature Reviews Psychology（引 350） |
| **successive relearning**：每次复习必须“回忆到正确”，不是看过；3–4 次成功回忆可维持数月 | Rawson & Dunlosky 2011/2013/2022（引 429/207/58） |
| 交错练习为什么有效 = **判别对比**（练“区分题型”）+ 分布练习 | Foster et al. 2019, Memory & Cognition（引 109） |
| 交错练习带来问题解决迁移 | Samani & Pan 2021, npj Science of Learning（引 83） |
| 掌握度估计（BKT / DKT / AKT） | Abdelrahman et al. 2023, ACM Computing Surveys（引 750） |

### 2.2 聚合出的最优解

把上面的证据落到你的场景，六条：

```text
① 初始间隔 = 距考试天数 × 10–20%，之后交给 FSRS 动态修正
   考试锚点：**2027 年度四川省考 ≈ 2026 年 12 月上旬笔试**
     （四川自 2023 起“每年一考”：10 月底公告 → 11 月初报名 → 12 月上旬笔试；
       2026 年度是 2025-12-06 考的，节奏很稳）
   → 距今天（2026-09-13）约 **83 天** → **初始间隔 8–17 天**（取中位 ~12 天）

② 每天先做“最可能忘的”：按 FSRS 的 R（可提取度）升序，不是随机抽卡

③ 每次复习必须“回忆到正确”（successive relearning）
   —— 模糊想起 / 看一眼答案 都算没掌握，当天重来

④ 排期时把**语义相近的知识点错开**（同一批里不放两个“削弱方式”类近义点）
   —— LECTOR 2025 指出的坑；考公大量近义术语（削弱/质疑、支持/加强）尤其致命

⑤ 题型用**交错练习**，机制是判别对比：混着出题，逼你分辨“这题该用哪一招”
   —— 按题型块刷题 = 分块练习，效果差（Rohrer & Taylor；Foster 2019）

⑥ 配比：逾期的优先、新学限额、题型固定混入（见 2.3 推送结构）
```

#### 为什么“两套对象两套机制”是对的（有证据）

| 对象 | 对应证据 | 机制 |
| --- | --- | --- |
| 知识点（陈述性） | 间隔效应 + 检索练习 + successive relearning | FSRS 排期 + 主动回忆 |
| 题型（程序性） | 交错练习的**判别对比**机制 + 迁移研究 | 交错练习 + 错因收敛 |

把题型塞进卡片复习之所以失败：**会做 ≠ 记得**，题型靠的是“认得出该用哪招”，而这是判别能力，不是记忆。

### 2.3 落点

```text
状态库   ~/.pi/kaogong/state.sqlite（每个知识点/题型一行：S、D、上次复习、历史评分）
调度     FSRS-6（纯数学公式，无重依赖），初始间隔按 ① 的系数
每日推送 launchd 定时（**每天早上 08:00**，已定）→ Telegram：
           今天到期 N 个
           ├ 必做：逾期最狠的 5 个（按 R 最低排序）
           ├ 建议：新学 3 个（按讲义进度）
           └ 交错：从 ≥2 个题型里各抽 1 题（防分块练习，练判别）
           · 同批内做语义去重（同一批不出现近义知识点）
           卡片带 Obsidian 链接，点开就是笔记
库内面板 Obsidian Tasks / Dataview：⏳ 到期日 + 逾期红标
```

---

## 3. 任务三：错题本 ↔ 知识点 ↔ 掌握验证　🔬 已按证据重做

### 3.1 证据基础（Google Scholar，2026-09-13）

| 结论 | 出处 | 对错题本的含义 |
| --- | --- | --- |
| 从错误学习需要三个条件：**察觉到自己错了** + 反馈引发**意外感** + 注意力被调动 | Metcalfe 2017, *Annual Review of Psychology*（引 955） | 不能“看完答案就算”，必须先自己重做 |
| **失败的检索尝试**反而提升后续学习 | Kornell, Hays & Bjork 2009, *JEP:LMC*（引 873） | 先做再学，做错不是浪费 |
| **高信心错误最容易被纠正**（hypercorrection），但**一周后会复发** | Butterfield & Metcalfe 2001（引 352）；Butler, Fazio & Marsh 2011（引 150） | 必须记录答题信心；高信心错误要缩短间隔、多轮复测 |
| 即时反馈对**低阶**学习更有效，延迟反馈对**高阶**更有效 | Van der Kleij et al. 2015, *Review of Educational Research*（引 1337） | 对错即时给，解释留到当天晚些 |
| **错误管理训练**（鼓励犯错 + 从错中学）优于防错训练，尤其对迁移 | Keith & Frese 2008, *J Applied Psych*（引 917）；Dyre et al. 2017（引 106） | 学习期允许错，模考期才防错 |
| 错因要分**程序性 vs 概念性**；概念性错误需要“概念转变”而不是重做 | Holmes et al. 2013（引 57）；Herholdt & Sapire 2014（引 153） | “按错因分流”有文献依据 |
| 看错误样例也能学，条件是**有引导解释 + 错误上下文清楚** | Dieterich et al. 2025, *Educational Psychology Review* | 解析要写“我当时为什么这么想” |
| 考后反思表（exam wrappers） | Lovett 2013；Gezer-Templeton 2017（引 117） | 每次模考后填一张反思表 |

### 3.2 错题笔记格式（含信心与错因两个关键字段）

```markdown
---
科目: 行测
模块: 资料分析
知识点: ["[[增长率比较]]", "[[两期比重]]"]
题型: 资料分析-增长率比较
来源: 2024 国考副省级 第 118 题
日期: 2026-09-13
我的答案: B
正确答案: D
答题信心: 高            # ← 新增：hypercorrection 的关键字段
耗时: 95s
错因: 概念性错误          # 程序性 / 概念性 / 粗心 / 超纲陷阱
状态: 待巩固
---

## ① 先自己重做（不看答案）
…                       # 新增：让“察觉”发生，否则学习不成立
## ② 我当时为什么会这么想
…                       # 新增：意外感靠这一步产生
## ③ 正确解法
…
## ④ 错因归类 + 下次怎么防
…
## ⑤ 关联
同知识点错题 / 变式训练 / 所属小节
```

### 3.3 错因分流（决定进哪个流程）

| 错因 | 处理 | 依据 |
| --- | --- | --- |
| **概念性**（理解错 / 记混） | **概念转变**：反例打脸 → 重构规则 → 举正例 → 变式验证 | Holmes 2013 |
| **程序性**（知道但步骤错 / 慢） | 进题型训练队列（交错练习） | Foster 2019 |
| **粗心 / 时间压力** | **不进复习队列**，进“限时正确率”指标 | Keith & Frese：防错属于模考期 |
| **超纲 / 陷阱题** | 归档，低优先 | — |

**优先级 = 信心 × 错因：**

```text
高信心 + 概念性错误   ← 最高优先级
   （hypercorrection 效应最强，但一周后会复发 → 缩短间隔、多轮复测）
低信心 + 概念性错误   ← 正常进队列
程序性错误           ← 进题型交错队列
粗心                 ← 不进队列，单独看限时正确率
```

大多数错题本死在“所有错题一视同仁”。

### 3.4 处理流程（顺序就是机制，不能乱）

```text
做题（允许错）
  → ① 立刻知道对错              ← 低阶反馈：即时
  → ② 当天自己重做一遍，不看答案   ← 让“察觉”发生（Metcalfe 条件一）
  → ③ 写下“我当时怎么想的”        ← 产生意外感（条件二）
  → ④ 对解析 + 归因             ← 高阶反馈：延迟到当天晚些/次日更有效
  → ⑤ 进间隔队列                ← 按 信心×错因 定初始间隔
```

### 3.5 知识点关联（零新基建）

- 显式：front-matter 的 `[[知识点]]` 双链 + tag
- 隐式：qmd 的 `notes` collection 语义检索 → 给错题自动找同知识点题、给知识点找相关错题
- 库内：smart-connections 插件同样的事，在 Obsidian 里直接点得到
- ⚠️ 排期时避开语义相近项（LECTOR 2025 的语义干扰）

### 3.6 掌握判定（不能只看“做过一遍”）

```text
三关，全过才标“掌握”：
第一关 变式题    同知识点、换设问/换数字 → 连续 2 次正确（间隔递增）
第二关 迁移题    同知识点、换题型或换情境 → 至少 1 次正确
第三关 保持测试  N 天后（由 FSRS 的 S 决定）→ 仍正确
任一次失败 → 状态回退（不清零，FSRS 自动缩短间隔）
```

额外：给每道题算**区分度** —— “会的人也会错”的题不是好检测题，用答题数据回归题目难度（IRT 2PL 简化版），把无效题剔出验证集。

---

## 4. 已定决策（2026-09-13）

| 问题 | 决定 |
| --- | --- |
| 提醒节奏 | **每天早上一次**（08:00 推） |
| 知识点粒度 | **折中两层**：讲义小节 = 骨架笔记（查阅用）；考点 = 卡片（记忆用） |
| 考试目标 | **四川省考**（2027 年度 ≈ 2026 年 12 月上旬笔试） |

**从考试日反推出来的硬数字：**

```text
今天             2026-09-13
考试            ≈ 2026-12-05（12 月上旬，周六）
剩余            约 83 天（≈ 12 周）

最优间隔系数   10–20% of 保持期（Cepeda 2008）
→ 首轮间隔     8–17 天，取中位 ≈ 12 天
→ 掌握一个考点 约需 4–5 次复习（successive relearning）
→ 83 天窗口    能安排 5–7 个复习轮次 —— 足够，但不能再拖
```

**折中两层具体长什么样：**

```text
骨架层（讲义小节）  考公/行测/判断推理/<小节>.md       长文，按标题切成笔记，查阅用
卡片层（考点）      考公/考点/<考点名>.md             一条一个考点，记忆用
                    front-matter: 科目 / 模块 / 所属小节[[链接]] / 题型 / 难度
```

卡片与骨架用小节笔记的双链绑在一起：**复习卡片时点得回原文，翻原文时看得到哪些考点已掌握。**

**为什么不能按“小节”当卡片**：一节里塞 10 多个考点，复习时说不清到底哪个不会。
**为什么不能全按“考点”切碎**：2000+ 张卡复习不完，而且考点离开上下文就变成孤立句子。

---

## 5. 落地顺序

```text
第 0 步  Vision 工具链 + 一条命令跑通              ✅ 完成
第 1 步  真实照片跑通 + 校准环节定型               ✅ 完成
第 2 步  Obsidian 目录结构（行测五大模块/申论五大题型） ✅ 完成
第 3 步  考点卡片落地（折中两层）                  ✅ 完成
第 4 步  FSRS 排期 + 每天早上 08:00 Telegram 推送   ✅ 完成
第 5 步  错题本（录入/分流/关联/掌握三关）          ✅ 完成
第 6 步  拿真实讲义跑量（用户发照片）              ← 现在唯一剩的
```

**三个 skill 已装**：

```text
kaogong-ingest   拍照 → 矫正/去红 → OCR → 对着原图校准 → 落库 → qmd update → 切卡片
kaogong-review   每日到期推送 → 评分回写 → 进度查询（含「待验证」区块）
kaogong-mistake  错题录入（问出信心与错因）→ 分流 → 关联 → 掌握三关
```

## 6. 实现细节

### 6.1 排期（第 4 步）

```text
算法    FSRS-4.5 核心公式（17 参数，完全公开稳定）
        没上 FSRS-6：6 版多的 w17-w20 主要改「同日复习」与「可训练衰减」，
        日频场景用不到，而我没拿到足够公式细节 —— 宁可用确定的 4.5

状态    写在卡片自己的 front-matter，不另建数据库：
          到期 / 稳定度 / 难度 / 复习次数 / 上次复习
        Obsidian 看得见、Dataview 查得到、git 记得住历史

考试锚点 首轮 = 12 天（83 天窗口 × 10-20%），把 w0-w2 等比缩放到该值

自检    review.py selftest —— 首轮间隔、评分单调性、R 随时间下降、
        R(S,S)=90%、难度夹在 [1,10]、次数累加

定时    ~/Library/LaunchAgents/com.kaogong.review.plist（每天 08:00）
```

### 6.2 错题本（第 5 步）

```text
录入    mistake.py new —— 强制「答题信心」与「错因」两个字段
        信心不从用户那里问出来就是废数据

分流    概念性 → 概念转变（反例打脸→重构规则→举正例→变式验证）
        程序性 → 题型交错队列
        粗心   → 不进复习队列，进「限时正确率」
        超纲   → 归档

三关    未掌握 → 变式中 → 迁移中 → 待保持 → 已掌握
        变式连续 2 次正确才进迁移；迁移通过后按稳定度排保持测试
        任何一关失败 → 退一级（不清零，FSRS 那边会自己缩短间隔）

关联    qmd search <词> -c notes
        ⚠️ 两个坑：不带 -c notes 会去搜 pi-memory；新建笔记后要先 qmd update

自检    mistake.py selftest —— 状态机全路径 + 失败退级 + 非法状态归一
```

### 6.3 代码结构（第四轮 review 后重构）

原来的问题：`review.py` 一个文件同时管排期算法、卡片读写、Telegram 传输、命令行；
vault 路径与状态路径硬编码在四个文件里；库函数直接 `SystemExit`。

```text
paths   路径唯一来源        errors  领域异常，只在 main() 翻成退出码
fsrs    排期（纯函数）       mastery 掌握三关（纯函数）
cards   卡片读写查找          notify  Telegram 传输
vault   front-matter/快照    review / mistake / split 三个命令行
```

好处：路径改一处、算法能单独验、换推送通道不用碰排期。

---

**待建部分（错题与知识点的自动分类）见 [docs/classify-plan.md](docs/classify-plan.md)。**

---

**一句话**：拍完发我 → 0.7 秒出 markdown → 我对着原图校准 → 进 Obsidian。
复习：FSRS + 考试日反推首轮 12 天 + 每天早上 08:00 推 + 语义相近的考点错开排。
错题按**错因**分流；掌握用**变式 + 迁移 + 保持**三关验证。

### 6.4 去重（2026-09-13 加，用户实拍三张验证过）

同一页被拍多次（换角度、补拍、糊了重拍）很常见。不去重就会重复页 → 重复卡 →
同一考点被排期多次，复习负担翻倍且两张卡互相干扰。

```text
判据一  文字 3-gram 相似度 >= 0.45
        实测：同页重拍 0.61（糊到不行 0.54），同一本书相邻页 0.04 —— 中间是干净分界
判据二  页脚「第N页」相同 + 相似度 >= 0.25（重拍糊了时的旁证）
作用域  批内（同一批两张同页）与跨次运行（与 _raw 里已处理的页比）
逃生口  --no-dedup（想用更好的照片重做某页时）
```

**卡片绝不去重**：18 张卡里有 23 对相似度 >0.5，但它们是同一组的兄弟条目
（同一套措辞的并列项），合并就是丢知识点。处理方式是**错开排期**：
同一分组的卡不同天出现；无分组信息时按文本相似度 0.15 兜底。

实测三张照片：全部判为重复（0.81 / 0.54 / 0.78），vault 零改动；
关掉去重的对照则写出 3 页（含一张重复）。
